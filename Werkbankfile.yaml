project:
  name: "Authentikt"
  id: "authentikt-lib"

services:
  - name: "backend"
    path_prefixes:
      - "/api"
    modes:
      local:
        port: 8080

  - name: "frontend"
    path_prefixes:
      - /
    modes:
      local:
        port: 5173

dependencies:
  keycloak: true